{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ns.cascadeprotocol.org/knowledge/v0/condition-progression.schema.json",
  "title": "Condition may progress to / is a risk factor for / is a complication of a condition",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "subject",
    "predicate",
    "object",
    "provenance"
  ],
  "properties": {
    "subject": {
      "allOf": [
        {
          "$ref": "defs.schema.json#/$defs/node"
        },
        {
          "type": "object",
          "required": [
            "system",
            "code",
            "display"
          ],
          "properties": {
            "system": {
              "enum": [
                "ICD-10-CM"
              ]
            }
          }
        }
      ]
    },
    "predicate": {
      "enum": [
        "may_progress_to",
        "risk_factor_for",
        "complication_of"
      ]
    },
    "object": {
      "allOf": [
        {
          "$ref": "defs.schema.json#/$defs/node"
        },
        {
          "type": "object",
          "required": [
            "system",
            "code",
            "display"
          ],
          "properties": {
            "system": {
              "enum": [
                "ICD-10-CM"
              ]
            }
          }
        }
      ]
    },
    "provenance": {
      "$ref": "defs.schema.json#/$defs/provenance"
    }
  }
}
